export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>Aqua Asset API</h1>
      <p>API em <code>/api/contact</code>. O site Vite consome esta URL via <code>VITE_API_URL</code>.</p>
    </main>
  );
}
